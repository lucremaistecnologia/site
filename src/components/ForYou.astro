---
import FeatureBlock from "./FeatureBlock.astro";
---

<section class="section-for-you">
  <div class="container">
    <h1>É para você?</h1>
    <p class="subtitle">
      Conecte-se facilmente com as principais ferramentas do mercado
    </p>

    <div class="content-blocks">
      <FeatureBlock
        iconPath="/icons/cancel.svg"
        title="Não é para você"
        centered={true}
      >
        <div class="description" slot="description">
          <ul>
            <li>
              <div class="icon-background">
                <img src="/icons/cancel.svg" alt="Cancel Icon" />
              </div>Tem faturamento abaixo de 200 mil por ano
            </li>
            <li>
              <div class="icon-background">
                <img src="/icons/cancel.svg" alt="Cancel Icon" />
              </div>Faz venda de conteúdo voltado para apostas
            </li>
            <li>
              <div class="icon-background">
                <img src="/icons/cancel.svg" alt="Cancel Icon" />
              </div>Faz venda de outros conteúdos impróprios
            </li>
          </ul>
        </div>
      </FeatureBlock>

      <FeatureBlock
        iconPath="/icons/check.svg"
        title="É para você"
        centered={true}
      >
        <div class="description" slot="description">
          <ul>
            <li>
              <div class="icon-background">
                <img src="/icons/check.svg" alt="Check Icon" />
              </div>Tem faturamento acima de 200 mil por ano
            </li>
            <li>
              <div class="icon-background">
                <img src="/icons/check.svg" alt="Check Icon" />
              </div>O produto que entrega tem resultados comprovados
            </li>
            <li>
              <div class="icon-background">
                <img src="/icons/check.svg" alt="Check Icon" />
              </div>Os produtos contribuem para uma sociedade e país melhor
            </li>
          </ul>
        </div>
      </FeatureBlock>
    </div>
  </div>
</section>

    <style>
      .section-for-you {
        padding: var(--space-xxl);
        text-align: center;
        background-color: var(--color-background);
        color: var(--color-text);
      }

      .section-for-you h1 {
        font-size: max(2.5em, 4cqw); /* Adjusted to match Project.md example */
        margin-bottom: 1cqw;
      }

      .section-for-you .subtitle {
        font-size: max(1.1em, 1.5cqw);
        color: var(--color-text);
        margin-bottom: var(--space-l);
      }

      .content-blocks {
        display: flex;
        justify-content: center;
        gap: var(--space-s);
        margin: 0 auto;
        max-width: 180cqb;
      }
      .description {
        container-type: inline-size;
        width: 100%;
      }

      ul {
        list-style: none;
        display: flex;
        flex-direction: column;
        gap: var(--space-s);
        width: 100cqw;
      }
      li {
        display: flex;
        align-items: center;
        font-size: max(0.9em, 1.5cqw);
        text-align: left;
      }
      img {
        height: 2.2cqb;
      }

      .icon-background {
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: var(--color-background);
        border-radius: 2cqb;
        border: var(--border);
        width: fit-content;
        padding: 0.5rem;
        margin-right: 1rem;
        width: 6cqb;
        height: 6cqb;
      }
      @media (max-width: 960px) {
        .content-blocks {
          flex-direction: column;
          align-items: center;
        }
    </style>